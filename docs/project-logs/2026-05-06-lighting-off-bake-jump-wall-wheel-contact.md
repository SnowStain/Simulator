# 2026-05-06 关灯去烘焙 / 平衡步兵腿收拢 / 穿墙修复 / 轮底贴地

- 局内光照关闭时，GPU 地形烘焙不再叠加场景点光，缓存会按光照状态重建，避免地图仍残留光影。
- 平衡步兵跳跃时腿部收拢；默认台阶跨越能力压到接近轮底 3cm，按住 `X` 才进入伸腿式跨台阶。
- 高速移动的静态碰撞分段更细，减少墙体被直接跨过去的情况。
- 轮子视觉接触采样改为更贴近实际地形，尽量保证轮底与地面直接接触。
- 验证：`dotnet build src/Simulator.ThreeD/Simulator.ThreeD.csproj -c Debug` 通过。
